# function

* 函数式编程

1. 纯函数

``` JS
          function Hello(name) {
            return `hello ${name}` 
          }
```

2. 高阶函数

``` JS
          function Hello(name) {
            return `hello ${name}` 
          }
```

3. 无副作用

``` JS
          function Hello(name) {
            return `hello ${name}` 
          }
```
